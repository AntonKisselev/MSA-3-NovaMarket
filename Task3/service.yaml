apiVersion: v1
kind: Service
metadata:
  name: novamarket-service
  labels:
    app: novamarket
spec:
  selector:
    app: novamarket
  ports:
    - port: 80
      name: http-api
      targetPort: 8080
      nodePort: 30080
  type: NodePort